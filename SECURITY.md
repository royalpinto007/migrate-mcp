# Security

Report vulnerabilities privately through GitHub Security Advisories. Do not open a public issue for a suspected vulnerability.

`migrate-mcp` does not sandbox repository code. Framework generation and inspection commands can load project configuration. Use it only with repositories you trust, use least-privilege database credentials, and never point generation at production.
