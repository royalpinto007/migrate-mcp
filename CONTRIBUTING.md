# Contributing

Use Node.js 22 or newer, run `npm ci`, then run `npm run check` before opening a pull request.

Adapter changes need fixtures and tests that reflect real framework behavior. Never infer support from an unrelated command, never introduce shell execution, and never add tools that apply or roll back migrations. Use Conventional Commits.
